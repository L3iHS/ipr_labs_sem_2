apiVersion: v1
kind: Service
metadata:
  name: task-model-service
  namespace: lab5
  labels:
    app: task-model
spec:
  type: NodePort
  selector:
    app: task-model
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
