apiVersion: v1
kind: Service
metadata:
  name: task-model-service
  labels:
    app: task-model
spec:
  type: ClusterIP
  selector:
    app: task-model
  ports:
    - name: http
      port: 80
      targetPort: 8000
