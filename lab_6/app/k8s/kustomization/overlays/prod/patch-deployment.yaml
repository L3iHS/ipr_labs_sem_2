apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-model
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: task-model
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
