apiVersion: v1
kind: Service
metadata:
  name: task-model-service
  namespace: lab7-app
  labels:
    app: task-model
spec:
  type: NodePort
  selector:
    app: task-model
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30083
